{
  "scripts": {
    "re-sdk": "cd ./backend && npm run build && npm run swagger && cd ../ && npm run SDK",
    "SDK": "kiota generate -l typescript -d swagger.json -c KrabsSDK -o ./frontend/src/krabs-sdk",
    "re-cli": "cd ./backend && npm run build && npm run cli-swagger && cd ../ && npm run CLI",
    "CLI": "openapi-generator-cli generate -i cli-swagger.json -g bash -o ./cli"
  }
}
